# Ray's CPBL Data Site v6.1.0｜全站 UI 重建

## 設計方向

- 保留既有資料結構、DOM id 與頁面功能，避免破壞爬蟲與前端資料接線。
- 重建全站共用外殼：導覽、手機選單、頁面脈絡、卡片、表格、表單與返回頂端。
- 統一淺色／深色模式、間距、圓角、陰影、狀態色與鍵盤焦點。
- 強化手機版：導覽改抽屜、首頁改單欄、表格可橫向瀏覽、比賽中心分頁維持吸頂。
- 尊重 prefers-reduced-motion，降低動畫造成的不適。

## 新增檔案

- `css/v6-ui.css`：全站 UI design system 與 responsive 規則。
- `js/v6-shell.js`：共用導覽、主題切換、手機選單、麵包屑、無障礙跳轉與返回頂端。

## 套用範圍

所有根目錄公開 HTML 頁面皆接入新版 UI，包括首頁、賽程、戰績、賽季、球隊、球員、比賽中心、二軍、搜尋、規則、關於與版本頁。

## 相容性策略

新版 UI 不移除既有元素、不改變資料容器 id，也不覆寫頁面資料邏輯。因此原有 `fetch()`、事件監聽、Match Center 智慧分析與爬蟲資料格式可繼續使用。
